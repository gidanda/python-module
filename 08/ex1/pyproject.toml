[project]
name = "loading"
version = "0.1.0"
description = "Matrix data analysis with dependency management"
authors = [
    {name = "hiroki goda",email = "hiroz000qop@gmail.com"}
]
requires-python = ">=3.10"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
